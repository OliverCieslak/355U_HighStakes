
#pragma once

enum RingDetectorState {
    BLUE_RING_DETECTED = -1,
    NO_RING_DETECTED = 0,
    RED_RING_DETECTED = 1
};

enum HookState {
    HOOK_DOWN = -1,
    HOOK_STOPPPED = 0,
    HOOK_UP = 1,
};